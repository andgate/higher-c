let x : y = 7

add2(x: Int): Int {
  return add(x, 2);
}

module Foo
{
  add3(x) { return add(x, 3); }
}

/*
class List(a)
{
  length(): int;
  copy<List l>(): l a;
  get(index: I32): a;
  set(index: I32, x: a): Void;
  update(index: I32, f: a -> b): Void;
}


mapList<List l, a, b>(f: a -> b, xs: l a): l b 
{
  let ys = xs.copy();
  for(int i = 0; i < l.length(); inc(i))
    ys.update( i, f );

  return ys;
}
*/

main(): Int
{
  return add2(2);
}
