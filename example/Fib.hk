-- Simple program that computes a fibbonacci number
-- and prints it
mod Hawk.Example.Fib

-- Link to printd from c
foreign ccall "printd" printd : Double -> ()


infixr 4 * /
infixr 5 + -

-- Define a fibonacci function
fib : Float -> Float
fib n =
  if n < 3
    then
      1

    else
      fib (x-1) + fib (x-2)

-- Program entry point, prints 40th fibonacci number
main : ()
main =
  printd (fib 40)