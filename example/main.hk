mod Test.Main

use Data.List
use Math.Trig(sin cos as T tan Arc(asin acos atan) as A) as M
use (System.IO System.Network)

ty Maybe :- Nothing | Just a

ty Vector
  :- var x :: a
  ,  var y :: a

val blueberry :: I32 = ()
var raspberry :: I16 = ()
var blackberry :: W64 = ()

fn fancy :: Foo a b c, Bar c b => a -> b -> c -> ()
| a b c = ()

fn foo :: I32 -> ()
  | x 
    * () = ()
    * () = ()
    * _ = ()
  | y = ()